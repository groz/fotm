@model FotM.Portal.ViewModels.ArmoryViewModel

@{
    ViewBag.Title = "Home Page";
}

@section scripts
{
    <!-- TODO: switch to release(minified) versions of scripts -->
    <script src="~/Scripts/knockout-3.1.0.debug.js"></script>
    <script src="~/Scripts/jquery.signalR-2.0.2.js"></script>
    <script src="~/signalr/hubs"></script>
    <script src="~/Scripts/Home/index.js"></script>
}

<h2>
    <span data-bind="text: bracket"></span>
    <span data-bind="text: region"></span>
</h2>


<div class="row">
    <table class="table table-striped table-condensed table-responsive table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Faction</th>
                <th>Player 1</th>
                <th>Player 2</th>
                <th>Player 3</th>
                <th>Rating</th>
                <th>Delta</th>
                <th>Updated</th>
            </tr>
        </thead>
        <tbody data-bind="foreach: model().TeamStatsViewModels">
                <tr data-bind="css: DeltaType">
                    <td data-bind="text: Rank"></td>
                    <td>
                        <img data-bind="attr:{src: FactionImageLink}" alt="FactionImage"/>
                    </td>
                    
                    <!-- ko foreach: Players -->
                    <td>
                        <img data-bind="attr:{src: RaceImageLink}" alt="RaceImage"/>
                        <img data-bind="attr:{src: ClassImageLink}" alt="ClassImage"/>
                        <img data-bind="attr:{src: SpecImageLink}" alt="SpecImage"/>
                        <span data-bind="text:Name"></span>
                        -
                        <span data-bind="text:Realm"></span>
                    </td>
                    <!-- /ko -->

                    <td data-bind="text: Rating"></td>
                    <td data-bind="text: RatingChange"></td>
                    <td data-bind="text: LocalUpdateTime"></td>
                </tr>
        </tbody>
    </table>
</div>

<h2>FotM setups</h2>

<div class="row">
    <div class="col-lg-5">
        <table class="table table-striped table-condensed table-responsive col-lg-1">
            <tr>
                <th>#</th>
                <th>Setup</th>
                <th>Percent</th>
            </tr>

            @foreach (var teamSetup in Model.TeamSetupsViewModels)
            {
                <tr>
                    <td>@teamSetup.Rank</td>
                    <td>
                        @for (int i = 0; i < teamSetup.BracketSize; i++)
                        {
                            @*<img src="@(@teamSetup.ClassImageLinks[i])" alt="RaceImage" />*@
                            <img src="@(@teamSetup.SpecImageLinks[i])" alt="SpecImage" />
                        }
                    </td>
                    <td>@teamSetup.Percent</td>
                </tr>
            }
        </table>
    </div>
</div>